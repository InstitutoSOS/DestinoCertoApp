<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context="institutosos.org.br.destinocerto.activity.detail.site.SiteActivity">
    <RelativeLayout
        android:id="@+id/detail_header"
        android:layout_width="fill_parent"
        android:layout_height="120dp">

        <TextView
            android:id="@+id/site_name"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textIsSelectable="true"
            android:textSize="30sp" />

        <TextView
            android:id="@+id/site_type"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_below="@id/site_name"
            android:textIsSelectable="true"
            android:textSize="18sp" />
    </RelativeLayout>

    <fragment
        android:id="@+id/package_info_list"
        android:name="institutosos.org.br.destinocerto.activity.detail.site.SiteListFragment"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@+id/detail_header"
        tools:layout="@layout/fragment_item_list" />
</RelativeLayout>
